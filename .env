# Backend service URLs (server-side only, used by Next.js rewrites)
# Override in .env.local for local development
# Override via Docker build args for containerized builds
USER_API_URL=http://localhost:8081
ACCOUNT_API_URL=http://localhost:8082
TRANSACTION_API_URL=http://localhost:8083
